server:
  port: 8083

spring:
  application:
    name: cloud-product-service
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848

logging:
  level:
    root: debug
  config: classpath:logback-spring.xml

